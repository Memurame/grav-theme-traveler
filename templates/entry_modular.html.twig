{% extends 'partials/base.html.twig' %}

{% block main %}
  <section class="section-headerimage" style="background-image: url('{{ page.media[page.header.image].cropZoom(1920,450).url }}')">
    <h1 class="heading">{{ page.header.title }}</h1>
  </section>
  <section class="page section-entry">
    <div class="actionbar">
      <div class="path">
        <a href="{{ page.parent().url() }}" class="back"><i class="fas fa-reply"></i> zurück</a>
      </div>
      <span class="date"><i class="fas fa-calendar-alt"></i> {{ page.date() | date('d. m. Y') }}</span>
    </div>
    <article class="content">
      <div class="row">
        {% for module in page.collection() %}
          {{ module.content }}
        {% endfor %}
      </div>
      <footer>
        {% if  page.taxonomy.tag %}
          <div class="tags">
            {% for tag in page.taxonomy.tag %}
              <a href="{{ page.parent().url() }}/tag{{ config.system.param_sep }}{{ tag }}">
                <span class="entry-tag">{{ tag }}</span>
              </a>
            {% endfor %}
          </div>
        {% endif %}
        {% include 'partials/socialshare.html.twig' %}
      </footer>
    </article>
  </section>

{% endblock %}
