


{% set image_raw =  page.header.image | split('/') %}
{% set image_name = image_raw | last %}
{% set image_dir =  (image_raw | slice(0, -1)) | join('/') %}
{% set image_path = ((page.find(image_dir).filePathClean | split('/')) | slice(0, -1)) | join('/') %}


